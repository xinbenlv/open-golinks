# 文档图像

`readme-tour.gif` 为首页产品导览；`claim-copy/` 为无主链接认领与 slug 复制的真实浏览器对比截图。素材使用本地虚构数据。

```text
assets/
├── readme-tour.gif
└── claim-copy/ -> README + before/after screenshots
```

`owner-avatar/` 为已有主人链接的 Jazzicon 前后对比，使用本地虚构数据。

`unowned-avatar/` 为无主入口从常驻文字变为空缺头像的 390px 对比与展开状态。
